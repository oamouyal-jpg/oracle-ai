# SQLite (default local dev)
DATABASE_URL="file:./dev.db"
# PostgreSQL (production)
# DATABASE_URL="postgresql://user:password@localhost:5432/oracle_ai"
# Optional — leave empty or remove for offline mock AI. Invalid keys auto-fallback to mocks.
# OPENAI_API_KEY="sk-your-real-key"
PORT=4000
CORS_ORIGIN="http://localhost:3000"
DEV_USER_ID="dev-user"
